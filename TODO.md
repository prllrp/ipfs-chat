salt +  password => public/priv  key
when sending a message, send the  public key +  a signature
when recieving a message, verify the signature
username and  password must  yield the keys everytime

username-> posted on the dag
with the  username and the salt and some other info prolly


get ipld  working
create a create  user function
need  to create the original node
with ipfs desktop?

maybe yes that sounds  right
a directory with a README and a file structure


